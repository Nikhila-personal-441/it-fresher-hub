[build]
  base = "src/frontend"
  command = "pnpm install --prefer-offline && pnpm build"
  publish = "dist"

# SPA fallback — redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
